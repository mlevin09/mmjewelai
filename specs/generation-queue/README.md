# Generation Task v1

`generation-task-v1.schema.json` is generated from the immutable provider-neutral
`GenerationTaskEnvelope`. Version `1.0.0` contains only the generation run, session, and organization
identifiers needed to reload trusted state from PostgreSQL.
