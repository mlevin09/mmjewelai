# Prompt package

Reserved for the versioned JewelAI prompt compiler and prompt artifacts.

Step 2 establishes only the package boundary; no executable prompt compiler is implemented here yet.
Future implementation must consume validated specification revisions, preserve locked constraints, record prompt/template versions, and keep provider-specific behavior outside the deterministic domain model.
